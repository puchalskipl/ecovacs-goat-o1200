# Dependency tree audited by pre-commit (pip-audit). Align with
# custom_components/ecovacs_goat/manifest.json and .github/workflows/validate.yml.
paho-mqtt==2.1.0
cryptography>=43.0.0
pytest
aiohttp
